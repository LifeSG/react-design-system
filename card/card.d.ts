import type { CardProps } from "./types";
/** A themed container surface for grouping related content. */
export declare const Card: ({ children, className, "data-testid": dataTestId, ...otherProps }: CardProps) => JSX.Element;
