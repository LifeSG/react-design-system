/// <reference types="react" />
import { CheckboxProps } from "./types";
export declare const Checkbox: ({ className, checked, disabled, onClick, onKeyPress, displaySize, ...otherProps }: CheckboxProps) => JSX.Element;
