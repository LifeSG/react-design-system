import { CheckboxProps } from "./types";
export declare const Checkbox: ({ className, checked, disabled, focusableWhenDisabled, indeterminate, displaySize, id, tabIndex, onChange, ...otherProps }: CheckboxProps) => JSX.Element;
