/// <reference types="react" />
import { CountdownTimerProps } from "./types";
export declare const CountdownTimer: ({ className, align, timer, notifyTimer, offset, mobileOffset, show, fixed, "data-testid": testId, onFinish, onNotify, onTick, ...otherProps }: CountdownTimerProps) => JSX.Element;
