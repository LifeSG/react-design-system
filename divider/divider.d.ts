import type { DividerProps } from "./types";
/**
 * A horizontal line that visually separates sections of content.
 *
 * Use `Divider` to draw a clear boundary between distinct groups of content on
 * a page or within a layout.
 */
export declare const Divider: ({ thickness, lineStyle, layoutType, color, className, xxsCols, xsCols, smCols, mdCols, lgCols, xlCols, xxlCols, ...otherProps }: DividerProps) => import("react/jsx-runtime").JSX.Element;
