"use client";
import { ImageButton } from "@lifesg/react-design-system/image-button";

export default function Story() {
    return (
        <ImageButton imgSrc="https://assets.life.gov.sg/react-design-system/invalid">
            Fallback
        </ImageButton>
    );
}
