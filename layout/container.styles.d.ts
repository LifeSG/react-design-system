import { ContainerType } from "./types";
interface StyleProps {
    $type: ContainerType;
    $stretch: boolean;
}
export declare const StyledContainer: import("styled-components").StyledComponent<"div", import("styled-components").DefaultTheme, StyleProps, never>;
export {};
