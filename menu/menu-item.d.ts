import { MenuItemProps } from "./types";
export declare const MenuItem: {
    ({ label, subLabel, children, "data-testid": testId, ...otherProps }: MenuItemProps): JSX.Element;
    displayName: string;
};
