const ns = "fds-day-cell";

export const root = ns;
export const cell = `${ns}__cell`;
export const otherMonth = `${ns}--other-month`;
export const today = `${ns}--today`;
export const disabled = `${ns}--disabled`;
export const selectedStart = `${ns}--selected-start`;
export const selectedEnd = `${ns}--selected-end`;
export const inRange = `${ns}--in-range`;
