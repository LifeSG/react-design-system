import React from "react";
import { CalendarManagerProps, CalendarManagerRef } from "./types";
export declare const CalendarManager: (props: CalendarManagerProps & React.RefAttributes<CalendarManagerRef>) => React.ReactElement | null;
