import { FormField } from "./form-field";
import { FormLabel } from "./form-label";
import { FormSelect } from "./form-select";

export const Form = {
    Field: FormField,
    Label: FormLabel,
    Select: FormSelect,
};
