/**
 * A mandatory banner displayed at the top of .gov.sg digital service pages.
 *
 * @keywords masthead, government, banner, header, govsg
 */
export interface MastheadProps {
    /** Specifies if the masthead should stretch to the maximum width of the screen, with a fixed padding. @default false */
    stretch?: boolean | undefined;
}
