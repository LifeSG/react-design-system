export const CELL_HEIGHT = 48; //the height of each cell in the grid (unit: px)
export const TIME_INDICATOR_WIDTH = 42; //the width of the time indicator column (unit: px)
export const HEADER_HEIGHT = 130; //the height of the header (unit: px)
export const SLOT_INTERVAL = 30; // minutes per slot
export const MIN_COLUMN_WIDTH = 280; //the min width of the column (unit: px)

// Constants for slot positioning in week view
export const SLOT_WIDTH = 30;
export const SLOT_GAP = 2;
export const MAX_VISIBLE_SLOTS = 3;
