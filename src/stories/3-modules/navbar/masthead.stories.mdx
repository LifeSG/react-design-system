import { Canvas, Meta, Preview, Story } from "@storybook/addon-docs";
import { Masthead } from "src/components";

<Meta title="modules/Masthead" />

# Masthead

All .gov.sg digital services are to implement the `Masthead` across all pages.

<Canvas>
    <Story name="Masthead">
        <Masthead />
    </Story>
</Canvas>

## Guidelines

1. It should be used at the **top left corner** of the page
2. Make sure you do not add the `Masthead` in a `Layout` component
