import { Button, Text } from "src/components";
import styled from "styled-components";

// =============================================================================
// STYLING
// =============================================================================
export const StyledButton = styled(Button.Default)`
    margin-top: 16px;
`;

export const StyledLink = styled(Text.Hyperlink.Default)`
    margin-top: 8px;
`;
