import type { TabListItemProps } from "./types";

// =============================================================================
// COMPONENT
// =============================================================================
// Declarative slot component — props are consumed by Tab.TabList, not rendered
export const TabListItem = (_props: TabListItemProps) => null;

TabListItem.displayName = "Tab.TabListItem";
