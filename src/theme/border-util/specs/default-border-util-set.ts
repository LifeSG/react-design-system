import { BorderUtilSet } from "../types";
import { dashedBorderStyle, solidBorderStyle } from "../utils";

export const DefaultBorderUtilSet: BorderUtilSet = {
    solid: solidBorderStyle,
    "dashed-default": dashedBorderStyle,
};
