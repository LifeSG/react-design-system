import { Meta } from "@storybook/addon-docs";
import {
    LoadingDots,
    LoadingSpinner,
    LoadingDotsSpinner,
} from "src/animations";
import { Secondary, Heading3, Heading4, Title } from "../storybook-common";
import {
    AnimationDisplay,
    AnimationItem,
    AnimationPane,
    Content,
} from "./doc-elements";
import { PropsTable } from "./props-table";

<Meta title="General/Animations/Overview" />

<Title>Animations</Title>

<Secondary>Overview</Secondary>

A component that renders an animation on the UI.

```tsx
import { <Component_name> } from "@lifesg/react-design-system/animations";
```

<Secondary>Collection</Secondary>

<AnimationDisplay>
    <AnimationItem>
        <AnimationPane>
            <LoadingSpinner />
        </AnimationPane>
        <Content>
            <code>{"<LoadingSpinner />"}</code>
            <p>
                An animation that can be shown in between page transitions or
                while waiting for fetching of data.
            </p>
        </Content>
    </AnimationItem>
    <AnimationItem>
        <AnimationPane>
            <LoadingDots />
        </AnimationPane>
        <Content>
            <code>{"<LoadingDots />"}</code>
            <p>
                An animation that can be shown in between page transitions or
                while waiting for fetching of data.
            </p>
        </Content>
    </AnimationItem>
    <AnimationItem>
        <AnimationPane>
            <LoadingDotsSpinner />
        </AnimationPane>
        <Content>
            <code>{"<LoadingDotsSpinner />"}</code>
            <p>
                An animation that can be shown in between page transitions or
                while waiting for fetching of data.
                <br />
                <br />
                This is a customisable animation. <a href="/docs/general-animations-customisable-animations--loading-dots-spinner">
                    See here
                </a>&nbsp;to find out more.
            </p>
        </Content>
    </AnimationItem>
</AnimationDisplay>

<Secondary>Usage</Secondary>

```tsx
import { LoadingSpinner } from "@lifesg/react-design-system/animations";

export const SomeComponent = () => {
    return (
        <div>
            <LoadingSpinner />
        </div>
    );
};
```

<Secondary>Component API</Secondary>

<PropsTable />
