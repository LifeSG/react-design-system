import { Canvas, Meta, Preview, Story } from "@storybook/addon-docs";
import {
    LoadingDots,
    LoadingSpinner,
    LoadingDotsSpinner,
} from "src/animations";
import { Secondary, Heading3, Heading4, Title } from "../storybook-common";
import {
    AnimationDisplay,
    AnimationItem,
    AnimationPane,
    Content,
} from "./doc-elements";
import { Color } from "src/color";
import { BaseTheme } from "src/theme";
import { PropsTable } from "./props-table";

<Meta title="General/Animations" />

<Title>Animations</Title>

<Secondary>Overview</Secondary>

<AnimationDisplay>
    <AnimationItem>
        <AnimationPane>
            <LoadingSpinner />
        </AnimationPane>
        <Content>
            <code>{"<LoadingSpinner />"}</code>
            <p>
                An animation that can be shown in between page transitions or
                while waiting for fetching of data.
            </p>
        </Content>
    </AnimationItem>
    <AnimationItem>
        <AnimationPane>
            <LoadingDots />
        </AnimationPane>
        <Content>
            <code>{"<LoadingDots />"}</code>
            <p>
                An animation that can be shown in between page transitions or
                while waiting for fetching of data.
            </p>
        </Content>
    </AnimationItem>
    <AnimationItem>
        <AnimationPane>
            <LoadingDotsSpinner />
        </AnimationPane>
        <Content>
            <code>{"<LoadingDotsSpinner />"}</code>
            <p>
                An animation that can be shown in between page transitions or
                while waiting for fetching of data.
            </p>
        </Content>
    </AnimationItem>
</AnimationDisplay>
<br />

<Secondary>Replace lottie color with color props</Secondary>

You can made use of color props in `LoadingDotsSpinner` to replace the default lottie color.

<Preview>
    <Story name="With color props">
        <AnimationDisplay>
            <AnimationItem>
                <AnimationPane>
                    <LoadingDotsSpinner
                        color={Color.Primary({ theme: BaseTheme })}
                        data-testid={"loading-spinner"}
                    />
                </AnimationPane>
            </AnimationItem>
            <AnimationItem>
                <AnimationPane>
                    <LoadingDotsSpinner
                        color={"#EF413D"}
                        data-testid={"loading-spinner"}
                    />
                </AnimationPane>
            </AnimationItem>
            <AnimationItem>
                <AnimationPane>
                    <LoadingDotsSpinner
                        color={"#FEAB10"}
                        data-testid={"loading-spinner"}
                    />
                </AnimationPane>
            </AnimationItem>
        </AnimationDisplay>
    </Story>
</Preview>

<br />
<Secondary>Usage</Secondary>

```tsx
import { LoadingSpinner } from "@lifesg/react-design-system/animations";

export const SomeComponent = () => {
    return (
        <div>
            <LoadingSpinner />
        </div>
    );
};
```

<PropsTable />
