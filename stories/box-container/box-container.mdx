import { Canvas, Meta } from "@storybook/blocks";
import { Heading3, Secondary, Title } from "../storybook-common";
import * as BoxContainerStories from "./box-container.stories";
import { PropsTable } from "./props-table";

<Meta of={BoxContainerStories} />

<Title>BoxContainer</Title>

<Secondary>Overview</Secondary>

Displays contents in a box styling layout that can be collapsible if specified.

```tsx
import { BoxContainer } from "@lifesg/react-design-system/box-container";
```

<Canvas of={BoxContainerStories.Default} />

<Heading3>Non-collapsible</Heading3>

<Canvas of={BoxContainerStories.NonCollapsible} />

<Heading3>With a display state</Heading3>

Specifying the display state allows you to render an icon alongside the title. It is good to indicate if the contents
of the `BoxContainer` has some special attention required.

There are currently 3 states:

-   `default` - where no icon will be rendered
-   `error` - a red icon
-   `warning` - an orange icon

In this example, we are using the `error` display state.

<Canvas of={BoxContainerStories.WithDisplayState} />

<Heading3>Specifying a custom call to action</Heading3>

You can also specify a custom call to action that will appear on the header of the `BoxContainer`.

<Canvas of={BoxContainerStories.WithCustomCallToAction} />

<Secondary>Component API</Secondary>

<PropsTable />
