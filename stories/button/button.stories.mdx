import { Canvas, Meta, Preview, Story } from "@storybook/addon-docs";
import { Button } from "src/button";
import { Secondary, Heading3, Title } from "../storybook-common";
import { Container } from "./doc-elements";
import { PropsTable } from "./props-table";

<Meta title="General/Button" component={Button} />

<Title>Button</Title>

<Secondary>Overview</Secondary>

The primary call to action component.

```tsx
import { Button } from "@lifesg/react-design-system/button";
```

The `Button` component comes in 2 sizes, a default and small size.

<Canvas>
    <Story name="Button">
        <Container>
            <Button.Default>Default</Button.Default>
            <Button.Default styleType="secondary">Secondary</Button.Default>
            <Button.Default styleType="light">Light</Button.Default>
            <Button.Default styleType="link">Link</Button.Default>
            <Button.Default disabled>Disabled</Button.Default>
        </Container>
        <br />
        <Container>
            <Button.Small>Small</Button.Small>
            <Button.Small styleType="secondary">Secondary</Button.Small>
            <Button.Small styleType="light">Light</Button.Small>
            <Button.Small styleType="link">Link</Button.Small>
            <Button.Small disabled>Disabled</Button.Small>
        </Container>
    </Story>
</Canvas>

<Heading3>Loading</Heading3>

<Preview>
    <Story name="Loading state">
        <Container>
            <Button.Default loading>Loading</Button.Default>
            <Button.Default styleType="secondary" loading>
                Loading
            </Button.Default>
            <Button.Default styleType="light" loading>
                Loading
            </Button.Default>
            <Button.Default styleType="link" loading>
                Loading
            </Button.Default>
            <Button.Default disabled loading>
                Loading
            </Button.Default>
        </Container>
        <br />
        <Container>
            <Button.Small loading>Loading</Button.Small>
            <Button.Small styleType="secondary" loading>
                Loading
            </Button.Small>
            <Button.Small styleType="light" loading>
                Loading
            </Button.Small>
            <Button.Default styleType="link" loading>
                Loading
            </Button.Default>
            <Button.Small disabled loading>
                Loading
            </Button.Small>
        </Container>
    </Story>
</Preview>

<Secondary>Component API</Secondary>

The component also inherits the [HTMLButtonElement](https://developer.mozilla.org/en-US/docs/Web/API/HTMLButtonElement).

<PropsTable />
