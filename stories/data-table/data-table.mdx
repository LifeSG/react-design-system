import { Canvas, Meta } from "@storybook/blocks";
import * as DataTableStories from "./data-table.stories";
import { PropsTable } from "./props-table";

<Meta of={DataTableStories} />

# Data Table

## Overview

Organises a collection of data into readable rows.

```tsx
import { DataTable } from "@lifesg/react-design-system/data-table";
```

<Canvas of={DataTableStories.Default} />

## Fixed column widths

This example illustrates how column widths can be fixed by setting the `width` style on headers.

<Canvas of={DataTableStories.FixedColumnWidths} />

## Sort indicators

A common use case is for rows to be sorted in some order.

> **Note:** The source code below is only an example and is not intended to be used directly in production

<Canvas of={DataTableStories.SortIndicators} />

## Multi-selection of rows

> **Note:** The updating of the selection state is handled by the consumer

<Canvas of={DataTableStories.MultiSelectionOfRows} />

## Disabled checkboxes

<Canvas of={DataTableStories.DisabledCheckboxes} />

## Alternating rows

<Canvas of={DataTableStories.AlternatingRows} />

## Action bar

<Canvas of={DataTableStories.ActionBar} />

## No rows

<Canvas of={DataTableStories.NoRows} />

## Loading state

<Canvas of={DataTableStories.LoadingState} />

## Component API

<PropsTable />
