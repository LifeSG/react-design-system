import { Canvas, Meta, Preview, Story } from "@storybook/addon-docs";
import { useState } from "react";
import { DateInput } from "src/date-input";
import { Form } from "src/form";
import {
    Heading3,
    Heading4,
    Secondary,
    StoryContainer,
    Title,
} from "../../storybook-common";
import { Container } from "../shared-doc-elements";
import { PropsTable } from "./props-table";

<Meta title="Form/DateInput" component={Form.DateInput} />

<Title>Form.DateInput</Title>

<Secondary>Overview</Secondary>

The field that allows a user to enter a date value.

```tsx
import { Form } from "@lifesg/react-design-system/form";
```

> **Important note**: This component currently does not do date value validation

<Canvas>
    <Story name="DateInput">
        <StoryContainer>
            <Container>
                <Form.DateInput
                    label="This is the date input without button"
                    withButton={false}
                />
                <Form.DateInput label="This is the date input with button" />
                <Form.DateInput
                    label="This is range date input with disabled dates"
                    variant="range"
                    withButton={false}
                    disabledDates={["2023-03-17", "2023-03-02"]}
                />
                <Form.DateInput
                    label="This is range date input with button"
                    variant="range"
                />
                <Form.DateInput
                    label="This is range date input with range selection"
                    variant="range"
                    between={["2023-03-01", "2023-03-20"]}
                />
                <Form.DateInput
                    label="This is the disabled state"
                    disabled
                    variant="range"
                />
                <Form.DateInput
                    label="This is the disabled state with value"
                    disabled
                    variant="range"
                    value="2023-02-10"
                    endValue="2023-04-20"
                />
                <Form.DateInput
                    label="This is the readonly state"
                    readOnly
                    variant="range"
                />
                <Form.DateInput
                    label="This is the error state"
                    errorMessage="Date is required"
                    variant="range"
                />
            </Container>
        </StoryContainer>
    </Story>
</Canvas>

<Heading3>Using the field as a standalone</Heading3>

In the case that you require the date input field as a standalone, you can do this.

```tsx
import { DateInput } from "@lifesg/react-design-system/date-input";
```

<Preview>
    <Story name="Standalone usage">
        <StoryContainer>
            <Container>
                <DateInput withButton={false} />
            </Container>
            <Container>
                <DateInput
                    withButton={true}
                    variant="range"
                    disabledDates={["2023-03-17", "2023-03-02"]}
                />
            </Container>
            <Container>
                <DateInput
                    withButton={false}
                    variant="range"
                    disabledDates={["2023-03-17", "2023-03-02"]}
                />
            </Container>
        </StoryContainer>
    </Story>
</Preview>

<Secondary>Component API</Secondary>

<PropsTable />
