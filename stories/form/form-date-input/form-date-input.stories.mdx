import { Canvas, Meta, Preview, Story } from "@storybook/addon-docs";
import { useState } from "react";
import dayjs from "dayjs";
import { DateInput } from "src/date-input";
import { Form } from "src/form";
import {
    Heading3,
    Heading4,
    Secondary,
    StoryContainer,
    Title,
} from "../../storybook-common";
import { Container } from "../shared-doc-elements";
import { PropsTable } from "./props-table";

<Meta title="Form/DateInput" component={Form.DateInput} />

<Title>Form.DateInput</Title>

<Secondary>Overview</Secondary>

A field that allows a user to enter a single date or date range.

```tsx
import { Form } from "@lifesg/react-design-system/form";
```

<Heading3>Using a default date input</Heading3>

<Canvas>
    <Story name="DateInput">
        <StoryContainer>
            <Container>
                <Form.DateInput label="This is the default date input" />
                <Form.DateInput
                    label="This is the date input without buttons"
                    withButton={false}
                />
                <Form.DateInput label="This is the disabled state" disabled />
                <Form.DateInput label="This is the readonly state" readOnly />
                <Form.DateInput
                    label="This is the error state"
                    errorMessage="Invalid date"
                />
            </Container>
        </StoryContainer>
    </Story>
</Canvas>

<Heading3>Using a range selection</Heading3>

You can use `variant='range'` to specify a range selection.

<Canvas>
    <Story name="Range input">
        {() => {
            const [startValue] = useState(dayjs().day(10).format("YYYY-MM-DD"));
            const [endValue] = useState(dayjs().day(20).format("YYYY-MM-DD"));
            return (
                <StoryContainer>
                    <Container>
                        <Form.DateInput
                            label="This is the default date range input"
                            variant="range"
                        />
                        <Form.DateInput
                            label="This is the date range input without button"
                            variant="range"
                            withButton={false}
                            value={startValue}
                            valueEnd={endValue}
                        />
                        <Form.DateInput
                            label="This is the disabled state"
                            disabled
                            variant="range"
                            value={startValue}
                            valueEnd={endValue}
                        />
                        <Form.DateInput
                            label="This is the readonly state"
                            readOnly
                            variant="range"
                            value={startValue}
                            valueEnd={endValue}
                        />
                        <Form.DateInput
                            label="This is the error state"
                            variant="range"
                            errorMessage="Invalid date"
                        />
                    </Container>
                </StoryContainer>
            );
        }}
    </Story>
</Canvas>

<Heading3>With disabled dates</Heading3>

You can use `disabledDates` to specify dates that are not available for selection.

<Canvas>
    <Story name="With disabled dates">
        {() => {
            const [disabledDates] = useState([
                dayjs().day(10).format("YYYY-MM-DD"),
                dayjs().day(15).format("YYYY-MM-DD"),
                dayjs().day(25).format("YYYY-MM-DD"),
            ]);
            return (
                <StoryContainer>
                    <Container>
                        <Form.DateInput
                            label="This is a date input with disabled dates"
                            disabledDates={disabledDates}
                        />
                        <Form.DateInput
                            label="This is a date range input with disabled dates"
                            variant="range"
                            disabledDates={disabledDates}
                        />
                    </Container>
                </StoryContainer>
            );
        }}
    </Story>
</Canvas>

<Heading3>Within date range</Heading3>

You can restrict selection to a date range using `between`.

<Canvas>
    <Story name="Within date range">
        {() => {
            const [between] = useState([
                dayjs().day(7).format("YYYY-MM-DD"),
                dayjs().day(21).format("YYYY-MM-DD"),
            ]);
            return (
                <StoryContainer>
                    <Container>
                        <Form.DateInput
                            label="This date input has restricted selection"
                            between={between}
                        />
                        <Form.DateInput
                            label="This date range input has restricted selection"
                            variant="range"
                            between={between}
                        />
                    </Container>
                </StoryContainer>
            );
        }}
    </Story>
</Canvas>

<Heading3>Using the field as a standalone</Heading3>

In the case that you require the date input field as a standalone, you can do this.

```tsx
import { DateInput } from "@lifesg/react-design-system/date-input";
```

<Preview>
    <Story name="Standalone usage">
        <StoryContainer>
            <DateInput />
            <br />
            <DateInput variant="range" />
        </StoryContainer>
    </Story>
</Preview>

<Secondary>Component API</Secondary>

<PropsTable />
