import { Canvas, Meta } from "@storybook/blocks";
import * as FormHistogramSliderStories from "./form-histogram-slider.stories";
import { PropsTable } from "./props-table";

<Meta of={FormHistogramSliderStories} />

# Form.Histogram

## Overview

A field that allows a user to select a numeric range. It displays a histogram
which reflects the number of results within each data bin.

```tsx
import { Form } from "@lifesg/react-design-system/form";
```

<Canvas of={FormHistogramSliderStories.Default} />

## With labels

<Canvas of={FormHistogramSliderStories.WithLabels} />

## Empty bins

If there are no results across any of the bins, you may specify a custom display.

<Canvas of={FormHistogramSliderStories.EmptyBins} />

## Rendering in grid layouts

You can also specify [ColDiv's](/docs/getting-started-layout-column-divs--column-divs)
column props to span across the specified columns in grid layouts.

<Canvas of={FormHistogramSliderStories.RenderingInGridLayout} />

## Using the field as a standalone

In the case that you require the HistogramSlider field as a standalone, you can do this.

```tsx
import { HistogramSlider } from "@lifesg/react-design-system/histogram-slider";
```

<Canvas of={FormHistogramSliderStories.StandaloneUsage} />

## Component API

<PropsTable />
