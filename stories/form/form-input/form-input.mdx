import { Canvas, Meta } from "@storybook/blocks";
import * as FormInputStories from "./form-input.stories";
import { PropsTable } from "./props-table";

<Meta of={FormInputStories} />

# Form.Input

## Overview

A field that allows a user to enter a response.

```tsx
import { Form } from "@lifesg/react-design-system/form";
```

<Canvas of={FormInputStories.Default} />

## Clear button

<Canvas of={FormInputStories.ClearButton} />

## Text spacing

<Canvas of={FormInputStories.TextSpacing} />

## Rendering in grid layouts

You can also specify [ColDiv's](/docs/getting-started-layout-column-divs--docs)
column props to span across the specified columns in grid layouts.

<Canvas of={FormInputStories.RenderingInGridLayout} />

## Using the field as a standalone

In the case that you require the input field as a standalone, you can do this.

```tsx
import { Input } from "@lifesg/react-design-system/input";
```

<Canvas of={FormInputStories.StandaloneUsage} />

## Component API

<PropsTable />
