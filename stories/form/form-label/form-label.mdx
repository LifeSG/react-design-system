import { Canvas, Meta } from "@storybook/blocks";
import { Heading3, Secondary, Title } from "../../storybook-common";
import * as FormLabelStories from "./form-label.stories";
import { PropsTable } from "./props-table";

<Meta of={FormLabelStories} />

<Title>Form.Label</Title>

<Secondary>Overview</Secondary>

This component is also available for use individually if you require to construct customised layouts.

```tsx
import { Form } from "@lifesg/react-design-system/form";
```

<Canvas of={FormLabelStories.Default} />

<Heading3>Real world usage</Heading3>

Given its standalone nature, the `Form.Label` can be used in any layouts or structures.

<Canvas of={FormLabelStories.RealWorldUsage} />

<Heading3>With custom child elements</Heading3>

You can also add other HTML elements within the form label.

<Canvas of={FormLabelStories.NestedChildren} />

<Secondary>Component API</Secondary>

<PropsTable />
