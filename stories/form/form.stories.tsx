import type { Meta } from "@storybook/react-webpack5";
import { Form } from "src/form";

const meta: Meta = {
    title: "Data Input/Form",
    tags: ["form", "fields", "validation", "label", "error message"],
};

export default meta;
