import { Canvas, Meta } from "@storybook/addon-docs/blocks";
import * as FullscreenImageCarouselStories from "./fullscreen-image-carousel.stories";
import { AutoArgTypesTabs } from "../storybook-common";

<Meta of={FullscreenImageCarouselStories} />

# FullscreenImageCarousel

## Overview

An overlay that display images in a carousel and offers the ability to zoom into the current image.

```tsx
import { FullscreenImageCarousel } from "@lifesg/react-design-system/fullscreen-image-carousel";
```

<Canvas of={FullscreenImageCarouselStories.Default} />

## Loading and error states

<Canvas of={FullscreenImageCarouselStories.LoadingAndErrorStates} />

## With many images

<Canvas of={FullscreenImageCarouselStories.WithManyImages} />

## With delete button

<Canvas of={FullscreenImageCarouselStories.WithDeleteButton} />

## Configurable

<Canvas of={FullscreenImageCarouselStories.Configurable} />

## With file info

<Canvas of={FullscreenImageCarouselStories.WithFileInfo} />

## With custom content

Slides can render arbitrary content via `renderContent`. If the content is not an image, you are recommended to set `itemLabel` so that the type of content can be described accurately to screen readers.

<Canvas of={FullscreenImageCarouselStories.WithCustomContent} />

## Custom actions

`customActions` adds your own icon buttons to the top bar, between the file info and the magnifier. An item can carry its own `customActions`, which **replaces** the component-level list for that slide rather than adding to it — pass an empty array to show no custom actions on a particular slide.

<Canvas of={FullscreenImageCarouselStories.WithCustomActions} />

## Component API

<AutoArgTypesTabs of={FullscreenImageCarouselStories} />
