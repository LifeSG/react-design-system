import { Meta } from "@storybook/addon-docs/blocks";
import { DocInfo } from "stories/storybook-common";
import { Installation } from "./doc-elements";

<Meta title="Getting started/Installation" />

# Installation

<DocInfo type="warning">
    {
        <p>
            You are referring to v4 of the design system. You can find the v3
            documentation{" "}
            <a
                href="https://designsystem.life.gov.sg/react/v3/index.html"
                target="_blank"
                rel="noreferrer"
            >
                here
            </a>
            .
        </p>
    }
</DocInfo>

### 1. Install the Flagship Design System package

```
npm i @lifesg/react-design-system
```

The following peer dependencies should be installed:

```json
{
    "@floating-ui/react": ">=0.26.23 <1.0.0",
    "@lifesg/react-icons": "^1.5.0",
    "react": "^17.0.2 || ^18.0.0 || ^19.0.0",
    "react-dom": "^17.0.2 || ^18.0.0 || ^19.0.0"
}
```

<Installation />

## AI agent skill

Give your AI coding assistant (Claude Code, Cursor, GitHub Copilot, Codex, and others) accurate knowledge of FDS components, import patterns, and usage rules.

```bash
npx skills add lifesg/react-design-system
```

Re-run after upgrading `@lifesg/react-design-system` to keep the skill in sync.
