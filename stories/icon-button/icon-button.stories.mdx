import { LockIcon } from "@lifesg/react-icons/lock";
import { Canvas, Meta, Preview, Story } from "@storybook/addon-docs";
import { IconButton } from "src/icon-button";
import { Secondary, Heading3, Title } from "../storybook-common";
import { PropsTable } from "./props-table";
import { Container, ContainerWrapper } from "./doc-elements";

<Meta title="General/IconButton" component={IconButton} />

<Title>Icon Button</Title>

<Secondary>Overview</Secondary>

A call to action component with an icon in it.

```tsx
import { IconButton } from "@lifesg/react-design-system/icon-button";
```

<Canvas>
    <Story name="IconButton">
        <IconButton>
            <LockIcon />
        </IconButton>
    </Story>
</Canvas>

<Heading3>Variations</Heading3>

The `IconButton` component comes in 3 sizes, a large, small and default size.

<Preview>
    <Story name="Variations">
        <ContainerWrapper>
            <Container>
                <span />
                <span>Large</span>
                <span>Default</span>
                <span>Small</span>
            </Container>
            <br />
            <Container>
                <label>Primary</label>
                <IconButton sizeType="large" buttonStyle="primary">
                    <LockIcon />
                </IconButton>
                <IconButton buttonStyle="primary">
                    <LockIcon />
                </IconButton>
                <IconButton sizeType="small" buttonStyle="primary">
                    <LockIcon />
                </IconButton>
            </Container>
            <br />
            <Container>
                <label>Secondary</label>
                <IconButton sizeType="large" buttonStyle="primary">
                    <LockIcon />
                </IconButton>
                <IconButton buttonStyle="secondary">
                    <LockIcon />
                </IconButton>
                <IconButton sizeType="small" buttonStyle="secondary">
                    <LockIcon />
                </IconButton>
            </Container>
            <br />
            <Container>
                <label>Light</label>
                <IconButton sizeType="large" buttonStyle="light">
                    <LockIcon />
                </IconButton>
                <IconButton buttonStyle="light">
                    <LockIcon />
                </IconButton>
                <IconButton sizeType="small" buttonStyle="light">
                    <LockIcon />
                </IconButton>
            </Container>
            <br />
            <Container>
                <label>Disabled</label>
                <IconButton sizeType="large" disabled>
                    <LockIcon />
                </IconButton>
                <IconButton disabled>
                    <LockIcon />
                </IconButton>
                <IconButton sizeType="small" disabled>
                    <LockIcon />
                </IconButton>
            </Container>
        </ContainerWrapper>
    </Story>
</Preview>

<Heading3>Using custom components</Heading3>

You can also pass in custom components to the `IconButton`. In this example, we will pass in an image.

<Preview>
    <Story name="Custom components">
        <IconButton>
            <img
                src="https://cdn-icons-png.flaticon.com/128/2171/2171991.png"
                alt="icon-image"
            />
        </IconButton>
    </Story>
</Preview>

<Secondary>Component API</Secondary>

<PropsTable />
