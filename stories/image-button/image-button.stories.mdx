import { Canvas, Meta, Preview, Story } from "@storybook/addon-docs";
import { useState } from "react";
import { ImageButton } from "src/image-button";
import { Secondary, Heading3, Title } from "../storybook-common";
import { PropsTable } from "./props-table";

<Meta title="General/ImageButton" component={ImageButton} />

<Title>Image Button</Title>

<Secondary>Overview</Secondary>

A call to action component with an image and label.

```tsx
import { ImageButton } from "@lifesg/react-design-system/image-button";
```

<Canvas>
    <Story name="ImageButton">
        <ImageButton imgSrc="https://cdn-icons-png.flaticon.com/512/4401/4401459.png">
            Office Equipment
        </ImageButton>
    </Story>
</Canvas>

<Heading3>Disabled</Heading3>

The button supports disabled state.

<Preview>
    <Story name="Disabled">
        <ImageButton
            disabled
            imgSrc="https://cdn-icons-png.flaticon.com/512/10754/10754537.png"
        >
            Disabled
        </ImageButton>
    </Story>
</Preview>

<Heading3>Selected</Heading3>

The button supports selected state.

<Preview>
    <Story name="Selected">
        {() => {
            const [selected, setSelected] = useState(false);
            return (
                <ImageButton
                    selected={selected}
                    onClick={() => setSelected(!selected)}
                    imgSrc="https://cdn-icons-png.flaticon.com/512/10754/10754537.png"
                >
                    Select Me
                </ImageButton>
            );
        }}
    </Story>
</Preview>

<Heading3>Error</Heading3>

The button supports error state.

<Preview>
    <Story name="Error">
        {() => {
            return (
                <ImageButton
                    error
                    imgSrc="https://cdn-icons-png.flaticon.com/512/10754/10754537.png"
                >
                    Error
                </ImageButton>
            );
        }}
    </Story>
</Preview>

<Heading3>Fallback</Heading3>

When the specified image fails to load, it is replaced by a fallback.

<Preview>
    <Story name="Fallback">
        <ImageButton
            disabled={false}
            imgSrc="https://cdn-icons-png.flaticon.com/512/10754/10754asdfads537.png"
            focusOutline="browser"
        >
            Rooms
        </ImageButton>
    </Story>
</Preview>

<Secondary>Component API</Secondary>

<PropsTable />
