import type { Meta } from "@storybook/react-webpack5";
import { InputMultiSelect } from "src/input-multi-select";

// eslint-disable-next-line @typescript-eslint/no-explicit-any
const meta: Meta<any> = {
    title: "Data Input/Input Multi Select",
    component: InputMultiSelect,
    tags: [
        "multi-select",
        "dropdown",
        "checkbox",
        "multiple-choice",
        "form",
        "select",
    ],
};

export default meta;
