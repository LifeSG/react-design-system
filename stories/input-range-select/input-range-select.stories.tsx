import type { Meta } from "@storybook/react-webpack5";
import { InputRangeSelect } from "src/input-range-select";

// eslint-disable-next-line @typescript-eslint/no-explicit-any
const meta: Meta<any> = {
    title: "Data Input/Input Range Select",
    component: InputRangeSelect,
    tags: [
        "range-select",
        "start-end",
        "dropdown-pair",
        "from-to",
        "select",
        "filter",
    ],
};

export default meta;
