import type { Meta } from "@storybook/react-webpack5";
import { InputSelect } from "src/input-select";

// eslint-disable-next-line @typescript-eslint/no-explicit-any
const meta: Meta<any> = {
    title: "Data Input/Input Select",
    component: InputSelect,
    tags: [
        "input-select",
        "dropdown",
        "select",
        "single-select",
        "combobox",
        "form",
    ],
};

export default meta;
