import { Meta, Secondary, Title } from "../storybook-common";
import * as InputStories from "./input.stories";

<Meta of={InputStories} />

<Title>Input</Title>

<Secondary>Overview</Secondary>

A single-line text entry field with an optional clear button, error state, and style variants. Extends standard HTML input attributes.

```tsx
import { Input } from "@lifesg/react-design-system/input";
```
