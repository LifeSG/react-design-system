import { Canvas, Meta } from "@storybook/addon-docs/blocks";
import * as LinkListStories from "./link-list.stories";
import { PropsTable } from "./props-table";

<Meta of={LinkListStories} />

# LinkList

## Overview

A component that displays a continuous group of link elements.

```tsx
import { LinkList } from "@lifesg/react-design-system/link-list";
```

<Canvas of={LinkListStories.Default} />

<Canvas of={LinkListStories.Small} />

## Minimizing the list

You are able to specify the number of items to show in the minimized view by using the `maxShown` property. Specifying
this property will allow you to show only the specified number of items while minimizing the rest.

<Canvas of={LinkListStories.MinimizedList} />

## Lazy loaded list

You can enable lazy loading by setting the `loadMode` to `lazy`.

> In this example, two items are added each time and stops after loading is
> triggered 3 times

<Canvas of={LinkListStories.LazyLoadedList} />

## Custom labels

You can customise the `View more` and `View less` labels.

<Canvas of={LinkListStories.CustomLabel} />

## Component API

<PropsTable />
