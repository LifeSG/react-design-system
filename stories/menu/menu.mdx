import { Canvas, Meta } from "@storybook/addon-docs/blocks";
import * as MenuStories from "./menu.stories";
import { AutoArgTypesTabs } from "../storybook-common";

<Meta of={MenuStories} />

# Menu

## Overview

A component which appears when hovering or clicking on a target element and
displays a menu. It is commonly used in combination with
[Avatar](/docs/navigation-avatar--docs) and
[Navbar](/docs/navigation-navbar--docs).

This module contains:

-   **Menu** - a utility component that handles the triggering of the menu, extending [PopoverTrigger](/docs/overlays-popover--docs)
-   **Menu.Content** - the main wrapper containing the menu items
-   **Menu.Section** - groups menu items and optionally shows a section label
-   **Menu.Item** - a menu item with optional label and sublabel
-   **Menu.Link** - a menu item that contains a hyperlink

```tsx
import { Menu } from "@lifesg/react-design-system/menu";
```

<Canvas of={MenuStories.Default} />

## Menu content

<Canvas of={MenuStories.MenuContent} />

## Multiple sections with mixed layouts

Sections with different layouts can be combined in a single `Menu.Content`. In this example a regular list section sits alongside a grid section.

<Canvas of={MenuStories.MultipleSections} />

## Grid layout

`Menu.Section` can display links in a multi-column grid by setting the `gridLayout` prop.

<Canvas of={MenuStories.GridLayout} />

## Grid layout with label

A section label can be combined with `columns`. The label spans the full width above the grid columns.

<Canvas of={MenuStories.GridLayoutWithLabel} />

## Grid layout with overflow scroll

When the panel height is constrained (e.g. via `maxHeight`), items that overflow are accessible by scrolling.

<Canvas of={MenuStories.GridLayoutScrollable} />

## Resize and flip

As the menu extends [PopoverTrigger](/docs/overlays-popover--docs), resize and
flip behaviour can be configured

<Canvas of={MenuStories.ResizeAndFlip} />

## Component API

<AutoArgTypesTabs of={MenuStories} />
