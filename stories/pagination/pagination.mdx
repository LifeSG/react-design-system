import { Canvas, Meta, Controls } from "@storybook/addon-docs/blocks";
import * as PaginationStories from "./pagination.stories";
import { AutoArgTypesTabs, toStoryExcludedProps } from "../storybook-common";
import { storybookArgTypesByTitle } from "../../.storybook/generated/storybook-argtypes.generated";

<Meta of={PaginationStories} />

# Pagination

## Overview

A component to provide navigation between pages.

```tsx
import { Pagination } from "@lifesg/react-design-system/pagination";
```

## Playground

<Canvas of={PaginationStories.Playground} />
<div className="controls-only">
    <Controls
        of={PaginationStories.Playground}
        exclude={toStoryExcludedProps(
            storybookArgTypesByTitle["Navigation/Pagination"]
        )}
    />
</div>

## Default

<Canvas of={PaginationStories.Default} />

## With many pages

When there are more items to display, only some pages are visible.

<Canvas of={PaginationStories.WithManyPages} />

## With first and last nav

The `showFirstAndLastNav` prop allows the user to jump directly to the first and last page.

<Canvas of={PaginationStories.WithFirstAndLastNav} />

## With page size changer

The `showPageSizeChanger` prop enables a dropdown to select the number of items shown on the page.

<Canvas of={PaginationStories.WithPageSizeChanger} />

## With custom page size options

Custom page sizes can also be provided.

<Canvas of={PaginationStories.WithCustomPageSizeOptions} />

## Component API

<AutoArgTypesTabs of={PaginationStories} />
