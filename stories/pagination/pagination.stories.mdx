import { Canvas, Meta, Preview, Story } from "@storybook/addon-docs";
import { useEffect, useState } from "react";
import { Pagination } from "src/pagination";
import {
    Heading3,
    Secondary,
    StoryContainer,
    Title,
} from "../storybook-common";
import { PropsTable } from "./props-table";

<Meta title="Modules/Pagination" component={Pagination} />

<Title>Pagination</Title>

<Secondary>Overview</Secondary>

A component to handle the Pagination for pages.

```tsx
import { Pagination } from "@lifesg/react-design-system/pagination-input";
```

<Canvas>
    <Story name="Pagination">
        {() => {
            const [selectedPage, setSelected] = useState(1);
            return (
                <StoryContainer>
                    <Pagination
                        onPageChange={(selected) => setSelected(selected)}
                        activePage={selectedPage}
                        selectedPage={selectedPage}
                        totalPages={50}
                    />
                </StoryContainer>
            );
        }}
    </Story>
</Canvas>

<br />
<Secondary>Usage examples</Secondary>

<Heading3>Simple Pagination</Heading3>

This is a common use case among web apps where it requires a Pagination bar.

<Canvas>
    <Story name="Simple Pagination">
        {() => {
            const [selectedPage, setSelected] = useState(1);
            return (
                <StoryContainer>
                    <Pagination
                        onPageChange={(selected) => setSelected(selected)}
                        activePage={selectedPage}
                        selectedPage={selectedPage}
                        totalPages={50}
                    />
                </StoryContainer>
            );
        }}
    </Story>
</Canvas>

<br />
<Heading3>With First And Last Nav</Heading3>

This is a common use case among web apps where it requires a Pagination bar.

<Canvas>
    <Story name="With First And Last Nav">
        {() => {
            const [selectedPage, setSelected] = useState(1);
            return (
                <StoryContainer>
                    <Pagination
                        onPageChange={(selected) => setSelected(selected)}
                        activePage={selectedPage}
                        selectedPage={selectedPage}
                        totalPages={50}
                        showFirstAndLastNav
                        siblingRange={0}
                    />
                </StoryContainer>
            );
        }}
    </Story>
</Canvas>

<br />

<Heading3>With Jumper</Heading3>

This is a common use case among web apps where it requires a Pagination bar.

<Canvas>
    <Story name="With Jumper">
        {() => {
            const [selectedPage, setSelected] = useState(1);
            return (
                <StoryContainer>
                    <Pagination
                        onPageChange={(selected) => setSelected(selected)}
                        activePage={selectedPage}
                        selectedPage={selectedPage}
                        totalPages={50}
                        showJumper
                    />
                </StoryContainer>
            );
        }}
    </Story>
</Canvas>

<br />

<Heading3>With Boundary Range</Heading3>

This is a common use case among web apps where it requires a Pagination bar.

<Canvas>
    <Story name="With Boundary Range">
        {() => {
            const [selectedPage, setSelected] = useState(1);
            return (
                <StoryContainer>
                    <Pagination
                        onPageChange={(selected) => setSelected(selected)}
                        activePage={selectedPage}
                        selectedPage={selectedPage}
                        totalPages={50}
                        boundaryRange={2}
                        itemsPerPage={1}
                        renderItem={() => {}}
                        siblingRange={0}
                    />
                </StoryContainer>
            );
        }}
    </Story>
</Canvas>

<br />

<Heading3>With Sibling Range</Heading3>

This is a common use case among web apps where it requires a Pagination bar.

<Canvas>
    <Story name="With Sibling Range">
        {() => {
            const [selectedPage, setSelected] = useState(1);
            return (
                <StoryContainer>
                    <Pagination
                        onPageChange={(selected) => setSelected(selected)}
                        activePage={selectedPage}
                        selectedPage={selectedPage}
                        totalPages={50}
                        itemsPerPage={1}
                        siblingRange={2}
                    />
                </StoryContainer>
            );
        }}
    </Story>
</Canvas>

<Secondary>Component API</Secondary>

<PropsTable />
