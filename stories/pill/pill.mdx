import { Canvas, Meta } from "@storybook/blocks";
import { Heading3, Heading4, Secondary, Title } from "../storybook-common";
import * as PillStories from "./pill.stories";
import { PropsTable } from "./props-table";

<Meta of={PillStories} />

<Title>Pill</Title>

<Secondary>Overview</Secondary>

A static element that is used to communicate selections, statuses and categories.

```tsx
import { Pill } from "@lifesg/react-design-system/pill";
```

<Canvas of={PillStories.Default} />

<Heading3>Pill with icon</Heading3>

<Canvas of={PillStories.WithIcon} />

<Heading3>Variations</Heading3>

The `Pill` comes in 2 display formats, `outline` and `solid` and there are 6 color variations that can be denoted using the `colorType` prop.

<Canvas of={PillStories.Variations} />

<Secondary>Component API</Secondary>

<PropsTable />
