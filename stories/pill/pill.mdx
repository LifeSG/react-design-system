import { Canvas, Meta, Controls } from "@storybook/addon-docs/blocks";
import * as PillStories from "./pill.stories";
import { AutoArgTypesTabs, toStoryExcludedProps } from "../storybook-common";
import { storybookArgTypesByTitle } from "../../.storybook/generated/storybook-argtypes.generated";

<Meta of={PillStories} />

# Pill

## Overview

A static element that is used to communicate selections, statuses and categories.

```tsx
import { Pill } from "@lifesg/react-design-system/pill";
```

## Playground

<Canvas of={PillStories.Playground} />
<div className="controls-only">
    <Controls
        of={PillStories.Playground}
        sort="alpha"
        exclude={toStoryExcludedProps(
            storybookArgTypesByTitle["Feedback indicators/Pill"]
        )}
    />
</div>

## Variants

<Canvas of={PillStories.Default} />

## Pill with icon

<Canvas of={PillStories.WithIcon} />

## Variations

The `Pill` comes in 2 display formats, `outline` and `solid` and there are 7 color variations that can be denoted using the `colorType` prop.

<Canvas of={PillStories.Variations} />

## Component API

<AutoArgTypesTabs of={PillStories} />
