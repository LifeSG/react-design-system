import { Canvas, Meta } from "@storybook/blocks";
import { Heading3, Secondary, Title } from "../storybook-common";
import * as SidenavStories from "./sidenav.stories";
import { PropsTable } from "./props-table";

<Meta of={SidenavStories} />

<Title>Sidenav</Title>

<Secondary>Overview</Secondary>

A component that renders navigation buttons on the side of a page.

```tsx
import { Sidenav } from "@lifesg/react-design-system/sidenav";
```

<Canvas of={SidenavStories.Default} />

<Heading3>Subitem drawer</Heading3>

The drawer is expanded when a menu item with subitems is hovered or selected. After a subitem is selected, the drawer will close.

<Canvas of={SidenavStories.SubitemDrawer} />

<PropsTable />
