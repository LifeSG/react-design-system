import { Canvas, Meta } from "@storybook/blocks";
import * as TagStories from "./tag.stories";
import { PropsTable } from "./props-table";

<Meta of={TagStories} />

# Tag

## Overview

A static or interactive element that is used to communicate selections, statuses and categories.

```tsx
import { Tag } from "@lifesg/react-design-system/tag";
```

<Canvas of={TagStories.Default} />

## Interactive tag

**Note**: In tablet and mobile viewports, interactive tags are displayed in a larger size to extend the touch target.

<Canvas of={TagStories.Interactive} />

## Tag with icon

<Canvas of={TagStories.WithIcon} />

## Variations

The `Tag` comes in 2 display formats, `outline` and `solid` and there are 6 color variations that can be denoted using the `colorType` prop.

<Canvas of={TagStories.Variations} />

## Component API

<PropsTable />
