import { Meta } from "@storybook/blocks";
import { LifeSGTheme } from "src/theme";
import { AnimationDisplay } from "../doc-elements";

<Meta title="Foundations/Animation/LifeSG" />

# LifeSG animation

This is the animation set used when the `animationScheme` is `"lifesg"`.

```tsx
const theme: ThemeSpec = {
    animationScheme: "lifesg",
    // ...other specifications
};
```

<AnimationDisplay theme={LifeSGTheme} />
