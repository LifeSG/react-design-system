import { Meta } from "@storybook/blocks";
import { LifeSGTheme } from "src/theme";
import { BorderDisplay } from "../doc-elements";

<Meta title="Foundations/Border/Default" />

# Default border

This is the border set used when the `borderScheme` is `"default"`.

```tsx
const theme: ThemeSpec = {
    borderScheme: "default",
    // ...other specifications
};
```

<BorderDisplay theme={LifeSGTheme} />
