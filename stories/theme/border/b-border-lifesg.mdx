import { Meta } from "@storybook/blocks";
import { LifeSGTheme } from "src/theme";
import { BorderDisplay } from "../doc-elements";

<Meta title="Foundations/Border/LifeSG" />

# LifeSG border

This is the border set used when the `borderScheme` is `"lifesg"`.

```tsx
const theme: ThemeSpec = {
    borderScheme: "lifesg",
    // ...other specifications
};
```

<BorderDisplay theme={LifeSGTheme} />
