import { Meta } from "@storybook/blocks";
import { LifeSGTheme } from "src/theme";
import { BreakpointDisplay } from "../doc-elements";

<Meta title="Foundations/Breakpoint/LifeSG" />

# LifeSG breakpoint

This is the breakpoint set used when the `breakpointScheme` is `"lifesg"`.

```tsx
const theme: ThemeSpec = {
    breakpointScheme: "lifesg",
    // ...other specifications
};
```

<BreakpointDisplay theme={LifeSGTheme} />
