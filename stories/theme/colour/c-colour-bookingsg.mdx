import { Meta } from "@storybook/addon-docs/blocks";
import { V3_BookingSGTheme } from "src/v3_theme";
import { PrimitiveColourDisplay, SemanticColourDisplay } from "../doc-elements";

<Meta title="Foundations/Colours/BookingSG" />

# BookingSG colours

These are the palettes used when the `colourScheme` is `"bookingsg"`.

```tsx
const theme: ThemeSpec = {
    colourScheme: "bookingsg",
    // ...other specifications
};
```

## Semantic colours

<SemanticColourDisplay theme={V3_BookingSGTheme} />

## Primitive colours

<PrimitiveColourDisplay theme={V3_BookingSGTheme} />
