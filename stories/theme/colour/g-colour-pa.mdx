import { Meta } from "@storybook/addon-docs/blocks";
import { V3_PATheme } from "src/v3_theme";
import { PrimitiveColourDisplay, SemanticColourDisplay } from "../doc-elements";

<Meta title="Foundations/Colours/PA" />

# PA colours

These are the palettes used when the `colourScheme` is `"pa"`.

```tsx
const theme: ThemeSpec = {
    colourScheme: "pa",
    // ...other specifications
};
```

## Semantic colours

<SemanticColourDisplay theme={V3_PATheme} />

## Primitive colours

<PrimitiveColourDisplay theme={V3_PATheme} />
