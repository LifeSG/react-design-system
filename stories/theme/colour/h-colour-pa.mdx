import { Meta } from "@storybook/blocks";
import { PATheme } from "src/theme";
import { PrimitiveColourDisplay, SemanticColourDisplay } from "../doc-elements";

<Meta title="Foundations/Colours/PA" />

# PA colours

These are the palettes used when the `colourScheme` is `"pa"`.

```tsx
const theme: ThemeSpec = {
    colourScheme: "pa",
    // ...other specifications
};
```

## Semantic colours

<SemanticColourDisplay theme={PATheme} />

## Primitive colours

<PrimitiveColourDisplay theme={PATheme} />
