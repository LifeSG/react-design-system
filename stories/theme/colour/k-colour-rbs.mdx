import { Meta } from "@storybook/addon-docs/blocks";
import { V3_RBSTheme } from "src/v3_theme";
import { PrimitiveColourDisplay, SemanticColourDisplay } from "../doc-elements";

<Meta title="Foundations/Colours/RBS" />

# RBS colours

These are the palettes used when the `colourScheme` is `"rbs"`.

```tsx
const theme: ThemeSpec = {
    colourScheme: "rbs",
    // ...other specifications
};
```

## Semantic colours

<SemanticColourDisplay theme={V3_RBSTheme} />

## Primitive colours

<PrimitiveColourDisplay theme={V3_RBSTheme} />
