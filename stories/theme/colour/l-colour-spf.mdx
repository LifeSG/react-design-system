import { Meta } from "@storybook/addon-docs/blocks";
import { V3_SPFTheme } from "src/v3_theme";
import { PrimitiveColourDisplay, SemanticColourDisplay } from "../doc-elements";

<Meta title="Foundations/Colours/SPF" />

# SPF colours

These are the palettes used when the `colourScheme` is `"spf"`.

```tsx
const theme: ThemeSpec = {
    colourScheme: "spf",
    // ...other specifications
};
```

## Semantic colours

<SemanticColourDisplay theme={V3_SPFTheme} />

## Primitive colours

<PrimitiveColourDisplay theme={V3_SPFTheme} />
