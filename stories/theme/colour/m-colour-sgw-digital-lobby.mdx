import { Meta } from "@storybook/addon-docs/blocks";
import { SGWDigitalLobbyTheme } from "src/theme";
import { PrimitiveColourDisplay, SemanticColourDisplay } from "../doc-elements";

<Meta title="Foundations/Colours/SGW Digital Lobby" />

# SGW Digital Lobby colours

These are the palettes used when the `colourScheme` is `"sgwdigitallobby"`.

```tsx
const theme: ThemeSpec = {
    colourScheme: "sgwdigitallobby",
    // ...other specifications
};
```

## Semantic colours

<SemanticColourDisplay theme={SGWDigitalLobbyTheme} />

## Primitive colours

<PrimitiveColourDisplay theme={SGWDigitalLobbyTheme} />
