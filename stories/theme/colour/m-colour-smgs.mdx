// TODO: update in BOOKINGSG-9203

import { Meta } from "@storybook/addon-docs/blocks";
import { SMGSTheme } from "src/theme";
import { PrimitiveColourDisplay, SemanticColourDisplay } from "../doc-elements";

<Meta title="Foundations/Colours/SMGS" />

# SMGS colours

These are the palettes used when the `colourScheme` is `"smgs"`.

```tsx
const theme: ThemeSpec = {
    colourScheme: "smgs",
    // ...other specifications
};
```

## Semantic colours

<SemanticColourDisplay theme={SMGSTheme} />

## Primitive colours

<PrimitiveColourDisplay theme={SMGSTheme} />
