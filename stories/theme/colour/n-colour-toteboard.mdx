import { Meta } from "@storybook/addon-docs/blocks";
import { ToteBoardTheme } from "src/theme";
import { PrimitiveColourDisplay, SemanticColourDisplay } from "../doc-elements";

<Meta title="Foundations/Colours/Tote Board" />

# Tote Board colours

These are the palettes used when the `colourScheme` is `"toteboard"`.

```tsx
const theme: ThemeSpec = {
    colourScheme: "toteboard",
    // ...other specifications
};
```

## Semantic colours

<SemanticColourDisplay theme={ToteBoardTheme} />

## Primitive colours

<PrimitiveColourDisplay theme={ToteBoardTheme} />
