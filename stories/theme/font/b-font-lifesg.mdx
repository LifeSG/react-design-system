import { Meta } from "@storybook/blocks";
import { LifeSGTheme } from "src/theme";
import { FontDisplay } from "../doc-elements";

<Meta title="Foundations/Font/LifeSG" />

# LifeSG font

This is the font set used when the `fontScheme` is `"lifesg"`.

```tsx
const theme: ThemeSpec = {
    fontScheme: "lifesg",
    // ...other specifications
};
```

<FontDisplay theme={LifeSGTheme} />
