import { Meta } from "@storybook/blocks";
import { MyLegacyTheme } from "src/theme";
import { DocInfo } from "stories/storybook-common";
import { FontDisplay } from "../doc-elements";

<Meta title="Foundations/Font/MyLegacy" />

# MyLegacy font

This is the font set used when the `fontScheme` is `"mylegacy"`.

```tsx
const theme: ThemeSpec = {
    fontScheme: "mylegacy",
    // ...other specifications
};
```

This stylesheet should be loaded in your app.

```
https://assets.life.gov.sg/react-design-system/v3/css/open-sans.css
```

<FontDisplay theme={MyLegacyTheme} />
