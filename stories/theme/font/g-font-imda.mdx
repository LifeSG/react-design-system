import { Meta } from "@storybook/addon-docs/blocks";
import { IMDATheme } from "src/theme";
import { DocInfo } from "stories/storybook-common";
import { FontDisplay } from "../doc-elements";

<Meta title="Foundations/Font/IMDA" />

# IMDA font

This is the font set used when the `fontScheme` is `"imda"`.

```tsx
const theme: ThemeSpec = {
    fontScheme: "imda",
    // ...other specifications
};
```

This stylesheet should be loaded in your app.

```
https://assets.life.gov.sg/react-design-system/v3/css/montserrat.css
```

<DocInfo>
    [Common
    ligatures](https://developer.mozilla.org/en-US/docs/Web/CSS/font-variant-ligatures)
    are **disabled** for this font
</DocInfo>

<FontDisplay theme={IMDATheme} />
