import { Meta } from "@storybook/addon-docs/blocks";
import { ToteBoardTheme } from "src/theme";
import { FontDisplay } from "../doc-elements";

<Meta title="Foundations/Font/Tote Board" />

# Tote Board font

This is the font set used when the `fontScheme` is `"toteboard"`.

```tsx
const theme: ThemeSpec = {
    fontScheme: "toteboard",
    // ...other specifications
};
```

These stylesheets should be loaded in your app.

```
// Inter
https://assets.life.gov.sg/react-design-system/v3/css/inter.css
```

<FontDisplay theme={ToteBoardTheme} />
