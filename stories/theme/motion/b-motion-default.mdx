import { Meta } from "@storybook/addon-docs/blocks";
import { V3_LifeSGTheme } from "src/v3_theme";
import { MotionDisplay } from "../doc-elements";

<Meta title="Foundations/Motion/Default" />

# Default motion

This is the motion set used when the `motionScheme` is `"default"`.

```tsx
const theme: ThemeSpec = {
    motionScheme: "default",
    // ...other specifications
};
```

<MotionDisplay theme={V3_LifeSGTheme} />
