import { Meta } from "@storybook/blocks";
import { LifeSGTheme } from "src/theme";
import { RadiusDisplay } from "../doc-elements";

<Meta title="Foundations/Radius/LifeSG" />

# LifeSG radius

This is the radius set used when the `radiusScheme` is `"lifesg"`.

```tsx
const theme: ThemeSpec = {
    radiusScheme: "lifesg",
    // ...other specifications
};
```

<RadiusDisplay theme={LifeSGTheme} />
