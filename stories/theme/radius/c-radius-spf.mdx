import { Meta } from "@storybook/addon-docs/blocks";
import { RadiusDisplay } from "../doc-elements";

<Meta title="Foundations/Radius/SPF" />

# SPF radius

This is the radius set used when the theme is `"spf"`.

<RadiusDisplay theme="spf" />
