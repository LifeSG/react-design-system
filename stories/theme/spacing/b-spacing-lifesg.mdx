import { Meta } from "@storybook/blocks";
import { LifeSGTheme } from "src/theme";
import { SpacingDisplay } from "../doc-elements";

<Meta title="Foundations/Spacing/Default" />

# Default spacing

This is the spacing set used when the `spacingScheme` is `"default"`.

```tsx
const theme: ThemeSpec = {
    spacingScheme: "default",
    // ...other specifications
};
```

<br />

<SpacingDisplay theme={LifeSGTheme} />
