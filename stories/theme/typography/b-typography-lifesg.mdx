import { Meta } from "@storybook/blocks";
import { LifeSGTheme } from "src/theme";
import { TypographyDisplay } from "../doc-elements";

<Meta title="Foundations/Typography/LifeSG" />

# LifeSG typography

This is the font set used when the `fontScheme` is `"lifesg"`.

```tsx
const theme: ThemeSpec = {
    fontScheme: "lifesg",
    // ...other specifications
};
```

<TypographyDisplay theme={LifeSGTheme} />
