import { Meta } from "@storybook/blocks";
import { BookingSGTheme } from "src/theme";
import { FontDisplay } from "../doc-elements";

<Meta title="Foundations/Typography/BookingSG" />

# BookingSG font

This is the font set used when the `fontScheme` is `"bookingsg"`.

```tsx
const theme: ThemeSpec = {
    fontScheme: "bookingsg",
    // ...other specifications
};
```

<FontDisplay theme={BookingSGTheme} />
