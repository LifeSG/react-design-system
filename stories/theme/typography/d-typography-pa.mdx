import { Meta } from "@storybook/blocks";
import { PATheme } from "src/theme";
import { FontDisplay } from "../doc-elements";

<Meta title="Foundations/Typography/PA" />

# PA font

This is the font set used when the `fontScheme` is `"pa"`.

```tsx
const theme: ThemeSpec = {
    fontScheme: "pa",
    // ...other specifications
};
```

<FontDisplay theme={PATheme} />
