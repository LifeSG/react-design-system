import { Canvas, Meta, Preview, Story } from "@storybook/addon-docs";
import { useState } from "react";
import { TimeSlotBar } from "src/time-slot-bar";
import { Secondary, Title } from "../storybook-common";
import { PropsTable } from "./props-table";
import { TIME_SLOTS } from "./time-slot-bar-data.tsx";

<Meta title="Modules/TimeSlotBar" component={TimeSlotBar} />

<Title>TimeSlotBar</Title>

<Secondary>Overview</Secondary>

Time range picker help users view availability of the timings for booking.

```tsx
import { TimeSlotBar } from "@lifesg/react-design-system/time-slot-bar";
```

<Canvas>
    <Story name="TimeSlotBar">
        {() => {
            const timeSlotBarProps = {
                startTime: "08:00",
                endTime: "19:00",
                slots: TIME_SLOTS,
                defaultTimeSlot: {
                    color: "#ECEFEF",
                    styleType: "stripes",
                    secondaryColor: "#E0E4E5",
                },
            };
            return <TimeSlotBar {...timeSlotBarProps} />;
        }}
    </Story>
</Canvas>

<Secondary>Component API</Secondary>

<PropsTable />
