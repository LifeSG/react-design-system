import { Canvas, Meta } from "@storybook/blocks";
import * as TimeSlotWeekViewStories from "./time-slot-week-view.stories";
import { PropsTable } from "./props-table";

<Meta of={TimeSlotWeekViewStories} />

# TimeSlotWeekView

## Overview

Displays a calendar component to view available time slots in a week.

```tsx
import { TimeSlotWeekView } from "@lifesg/react-design-system/time-slot-week-view";
```

<Canvas of={TimeSlotWeekViewStories.Default} />

## With disabled dates

You can use `disabledDates` to specify dates that are not available for selection.

_(In this example,2 days before today and 2 days after today are disabled)_

<Canvas of={TimeSlotWeekViewStories.WithDisabledDates} />

## Within minimum and maximum dates

You can specify the `minDate` and/or `maxDate` to control the selection range.

<Canvas of={TimeSlotWeekViewStories.MinAndMaxDates} />

## Hiding nav header

You can remove the navigation header, including the month, year dropdown.

<Canvas of={TimeSlotWeekViewStories.HideNavHeader} />

## Disable day selection

<Canvas of={TimeSlotWeekViewStories.DisableDaySelection} />

## Handle slot selection

<Canvas of={TimeSlotWeekViewStories.SlotSelection} />

## Component API

<PropsTable />
