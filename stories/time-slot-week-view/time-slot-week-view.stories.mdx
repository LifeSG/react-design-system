import { Canvas, Meta, Preview, Story } from "@storybook/addon-docs";
import dayjs from "dayjs";
import { useState } from "react";
import {
    Heading3,
    Heading4,
    FullWidthStoryContainer,
    Secondary,
    Title,
} from "../storybook-common";
import { TimeSlotWeekView } from "src/time-slot-week-view";
import { Layout } from "src/layout";
import { PropsTable } from "./props-table";
import { getSlots, getSlotsMin } from "./slots";
import { Wrapper } from "./doc-elements";

<Meta
    title="Modules/TimeSlotWeekView"
    component={TimeSlotWeekView}
    parameters={{ layout: "padded" }}
/>

<Title>TimeSlotWeekView</Title>

<Secondary>Overview</Secondary>

Displays a calendar component.

```tsx
import { TimeSlotWeekView } from "@lifesg/react-design-system/time-slot-week-view";
```

<Canvas>
    <Story name="Default">
        <FullWidthStoryContainer>
            <Wrapper>
                <TimeSlotWeekView slots={getSlots()} />
            </Wrapper>
        </FullWidthStoryContainer>
    </Story>
</Canvas>

<Heading3>With disabled dates</Heading3>

You can use `disabledDates` to specify dates that are not available for selection.

<Canvas>
    <Story name="With disabled dates">
        {() => {
            const [disabledDates] = useState([
                dayjs().add(1, "day").format("YYYY-MM-DD"),
                dayjs().add(3, "day").format("YYYY-MM-DD"),
            ]);
            return (
                <FullWidthStoryContainer>
                    <Wrapper>
                        <TimeSlotWeekView
                            slots={getSlots()}
                            disabledDates={disabledDates}
                        />
                    </Wrapper>
                </FullWidthStoryContainer>
            );
        }}
    </Story>
</Canvas>

<Heading3>Within date range</Heading3>

You can restrict selection to a date range using `between`.

<Canvas>
    <Story name="Within date range">
        {() => {
            const minDate = dayjs().add(1, "day").format("YYYY-MM-DD");
            const maxDate = dayjs().add(4, "day").format("YYYY-MM-DD");
            return (
                <FullWidthStoryContainer>
                    <Wrapper>
                        <TimeSlotWeekView
                            slots={getSlots()}
                            minDate={minDate}
                            maxDate={maxDate}
                        />
                    </Wrapper>
                </FullWidthStoryContainer>
            );
        }}
    </Story>
</Canvas>

<Heading3>Without Navigation Header</Heading3>

You can remove navigation header including month,year dropdown.

<Canvas>
    <Story name="Without Nav Header">
        {() => {
            return (
                <FullWidthStoryContainer>
                    <Wrapper>
                        <TimeSlotWeekView
                            slots={getSlotsMin()}
                            showNavigationHeader={false}
                        />
                    </Wrapper>
                </FullWidthStoryContainer>
            );
        }}
    </Story>
</Canvas>

<Heading3>With Selection Disabled</Heading3>

You can disable date selection for this component.

<Canvas>
    <Story name="With Selection Disabled">
        {() => {
            return (
                <TimeSlotWeekView slots={getSlots()} enableSelection={false} />
            );
        }}
    </Story>
</Canvas>

<Heading3>With Slotclick handler</Heading3>

You can disable date selection for this component.

<Canvas>
    <Story name="With SlotClick handler">
        {() => {
            return (
                <FullWidthStoryContainer>
                    <Wrapper>
                        <TimeSlotWeekView
                            slots={getSlots()}
                            onSlotClick={(date, slot) => {
                                alert(date + JSON.stringify(slot));
                            }}
                        />
                    </Wrapper>
                </FullWidthStoryContainer>
            );
        }}
    </Story>
</Canvas>

<Secondary>Component API</Secondary>

<PropsTable />
