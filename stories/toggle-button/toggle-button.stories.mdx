import { Canvas, Meta, Preview, Story } from "@storybook/addon-docs";
import { Alert } from "src/alert";
import { ToggleButton } from "src/toggle-button";
import { Text } from "src/text";
import { Secondary, Heading3, Heading4, Title } from "../storybook-common";
import { PropsTable } from "./props-table";
import { Container } from "./doc-elements";

<Meta title="Data Input/ToggleButton (deprecated)" component={ToggleButton} />

<Title>ToggleButton</Title>
<Alert type="warning">
    <p>
        This component will be deprecated in <strong>v1.0.0</strong>. Use{" "}
        <a href="/docs/data-input-toggle--toggle">Toggle</a> instead.
    </p>
</Alert>
<br />

<Secondary>Overview</Secondary>

A button that allows a user to enter a response by clicking, and shown with a different state when activated.

```tsx
import { ToggleButton } from "@lifesg/react-design-system/toggle-button";
```

The `ToggleButton` comes in 3 states, and is controlled by the `checked` prop.

| State    |        Description        | `checked` value |
| -------- | :-----------------------: | --------------: |
| Default  | When no selection is made |     `undefined` |
| Active   |      When you select      |          `true` |
| Inactive |     When you unselect     |         `false` |

<Canvas>
    <Story name="ToggleButton">
        <Container>
            <li>
                <ToggleButton>Default</ToggleButton>
            </li>
            <li>
                <ToggleButton checked={true}>Active</ToggleButton>
            </li>
            <li>
                <ToggleButton checked={false}>Inactive</ToggleButton>
            </li>
        </Container>
    </Story>
</Canvas>

<Heading3>Disabled state</Heading3>

<Preview>
    <Story name="Disabled state">
        <Container>
            <li>
                <ToggleButton disabled={true}>Unselected</ToggleButton>
            </li>
            <li>
                <ToggleButton checked={true} disabled={true}>
                    Selected
                </ToggleButton>
            </li>
        </Container>
    </Story>
</Preview>

<Heading3>Using as a radio button</Heading3>

The `ToggleButton` can be also be used like a radio button. The behaviour can be controlled by the `type` prop.
By default, it works as a checkbox.

<Preview>
    <Story name="As a radio button">
        <Container>
            <li>
                <ToggleButton type="radio">Radio button</ToggleButton>
            </li>
            <li>
                <ToggleButton>Checkbox (default)</ToggleButton>
            </li>
        </Container>
    </Story>
</Preview>

<Secondary>Component API</Secondary>

The component also inherits the [HTMLInputElement](https://developer.mozilla.org/en-US/docs/Web/API/HTMLInputElement) attributes.

<PropsTable />
