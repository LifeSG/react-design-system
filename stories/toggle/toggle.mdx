import { Canvas, Meta } from "@storybook/blocks";
import { Heading3, Secondary, Title } from "../storybook-common";
import * as ToggleStories from "./toggle.stories";
import { PropsTable } from "./props-table";

<Meta of={ToggleStories} />

<Title>Toggle</Title>

<Secondary>Overview</Secondary>

A button that allows a user to enter a response by clicking, and shown with a different state when activated.

```tsx
import { Toggle } from "@lifesg/react-design-system/toggle";
```

<Canvas of={ToggleStories.Default} />

<Heading3>Behaviours</Heading3>

The `Toggle` can be used as a checkbox or radio button. This can be determined by the `type` prop.

| Type       | Behaviour                         |
| ---------- | --------------------------------- |
| `checkbox` | Checkbox, deselection is allowed  |
| `radio`    | Radio, deselection is not allowed |
| `yes`/`no` | Radio, deselection is not allowed |

Click to see the interactions.

<Canvas of={ToggleStories.Behaviours} />

<Heading3>Variants</Heading3>

Click on the links to see the variants in depth.

-   [With indicator](?path=/docs/data-input-toggle-with-indicator--checkbox-type)
-   [No border](?path=/docs/data-input-toggle-no-border--default-story)
-   [With sub label](?path=/docs/data-input-toggle-with-sub-label--with-sub-label)

<br />

<Secondary>Component API</Secondary>

<PropsTable />
