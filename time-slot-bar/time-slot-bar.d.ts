/// <reference types="react" />
import { TimeSlotBarProps } from "./types";
export declare const TimeSlotBar: ({ "data-testid": testId, className, variant, startTime, endTime, slots, onSlotClick, onClick, styleAttributes, }: TimeSlotBarProps) => JSX.Element;
