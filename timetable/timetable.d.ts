/// <reference types="react" />
import { TimeTableProps } from "./types";
export declare const TimeTable: ({ date, emptyContent, rowData, loading, minTime, maxTime, minDate, maxDate, totalRecords, onPage, onRefresh, onNextDayClick, onPreviousDayClick, ...otherProps }: TimeTableProps) => JSX.Element;
